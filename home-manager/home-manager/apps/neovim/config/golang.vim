" == PLUGIN: GOLANG ============================================================
" We disable gopls here because nvim-lspconfig will runs it.
let g:go_def_mode='godef'
let g:go_info_mode=''
let g:go_gopls_enabled = 0
